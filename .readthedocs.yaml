version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - git clone https://github.com/zarr-developers/jzarr.git external
      - rsync -av external/docs/ docs/

sphinx:
  configuration: docs/conf.py