version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    post_checkout:
      - git submodule update --init --recursive

sphinx:
  configuration: docs-src/docs/conf.py

python:
  install:
    - method: pip
      packages:
        - sphinx-rtd-theme